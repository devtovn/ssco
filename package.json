{
  "name": "ssco-monorepo",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test -c tests/e2e/playwright.config.ts",
    "test:load": "artillery run tests/load/artillery-search.yml"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "artillery": "^2.0.21"
  }
}
